const { MODULESPECIFIER_TYPES } = require('@babel/types');
const bcrypt = require('bcrypt');

class Router {
     constructor(app, db) {
          super(props);
          this.login(app, db)
     }
     render() { 
          return (  );
     }
}
 
module.exports = Router;